from django.shortcuts import render
from django.http import HttpResponse			

# Create your views here.
def index(request):
	service_name = Service.
	return HttpResponse("You have reached the catalog")

def s_detail(request):
	return HttpResponse("You are looking at the details of Service")

def layer(request):
	return HttpResponse("Welcome to the layer index")
	